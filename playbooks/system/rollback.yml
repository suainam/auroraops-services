# Generated by scripts/generate_ansible_playbooks.py
# Optimized Single-Play structure for high performance
- name: 回滚 system
  hosts: all
  gather_facts: true
  tags:
  - rollback
  - system
  tasks:
  - name: 回滚 system.zerotier
    include_role:
      name: vps.system.zerotier
      tasks_from: rollback
      apply:
        tags:
        - rollback
        - phase2
        - system
        - zerotier
    when: auroraops_roles['system']['zerotier'] | default(true) | bool
    tags:
    - rollback
    - phase2
    - system
    - zerotier
  - name: 回滚 system.ansibletest
    include_role:
      name: vps.system.ansibletest
      tasks_from: rollback
      apply:
        tags:
        - rollback
        - phase3
        - system
        - ansibletest
    when: auroraops_roles['system']['ansibletest'] | default(true) | bool
    tags:
    - rollback
    - phase3
    - system
    - ansibletest
    - ansibletest_install
    - ansibletest_run
    - ansibletest_status
    - ansibletest_result
    - ansibletest_cleanup
  vars_files:
  - ../../secrets/vault.yml
