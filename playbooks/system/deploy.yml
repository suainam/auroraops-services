# Generated by scripts/generate_ansible_playbooks.py
# Optimized Single-Play structure for high performance
- name: 部署 system
  hosts: all
  gather_facts: true
  tags:
  - deploy
  - system
  tasks:
  - name: 部署 system.zerotier
    include_role:
      name: vps.system.zerotier
      tasks_from: main
      apply:
        tags:
        - deploy
        - phase2
        - system
        - zerotier
    when: auroraops_roles['system']['zerotier'] | default(true) | bool
    tags:
    - deploy
    - phase2
    - system
    - zerotier
  - name: 部署 system.ansibletest
    include_role:
      name: vps.system.ansibletest
      tasks_from: main
      apply:
        tags:
        - deploy
        - phase3
        - system
        - ansibletest
    when: auroraops_roles['system']['ansibletest'] | default(true) | bool
    tags:
    - deploy
    - phase3
    - system
    - ansibletest
    - ansibletest_install
    - ansibletest_run
    - ansibletest_status
    - ansibletest_result
    - ansibletest_cleanup
  vars_files:
  - ../../secrets/vault.yml
