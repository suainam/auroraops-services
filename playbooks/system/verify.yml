# Generated by scripts/generate_ansible_playbooks.py
# Optimized Single-Play structure for high performance
- name: 验证 system
  hosts: all
  gather_facts: true
  tags:
  - verify
  - system
  tasks:
  - name: 验证 system.zerotier
    include_role:
      name: vps.system.zerotier
      tasks_from: verify
      apply:
        tags:
        - verify
        - phase2
        - system
        - zerotier
    when: auroraops_roles['system']['zerotier'] | default(true) | bool
    tags:
    - verify
    - phase2
    - system
    - zerotier
  - name: 验证 system.ansibletest
    include_role:
      name: vps.system.ansibletest
      tasks_from: verify
      apply:
        tags:
        - verify
        - phase3
        - system
        - ansibletest
    when: auroraops_roles['system']['ansibletest'] | default(true) | bool
    tags:
    - verify
    - phase3
    - system
    - ansibletest
    - ansibletest_install
    - ansibletest_run
    - ansibletest_status
    - ansibletest_result
    - ansibletest_cleanup
  vars_files:
  - ../../secrets/vault.yml
