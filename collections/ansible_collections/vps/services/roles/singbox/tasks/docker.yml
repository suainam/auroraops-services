---
- name: Sing-box Docker - Deploy container via community.docker
  become: true
  community.docker.docker_container:
    name: singbox
    image: "{{ singbox_image }}"
    # The image entrypoint is a bare `sing-box`; without an explicit run
    # command it prints help and exits, leaving the container in a restart loop.
    command: run -C /etc/sing-box
    state: started
    restart_policy: unless-stopped
    network_mode: host
    volumes:
      - "{{ singbox_config_dir }}:/etc/sing-box:ro"
      - "{{ singbox_data_dir }}/web:/opt/dockers/singbox/web:ro"
      - "/etc/letsencrypt:/etc/letsencrypt:ro"
  when: not ansible_check_mode
  tags: [deploy, services, singbox, phase5]
