# auroraops-services Context

## 1. 目标与定位
`auroraops-services` 承载 AuroraOps 所有按需选配的服务能力，包括网络打通、应用部署、Docker/Nginx 栈、以及操作者环境定制。

## 2. 核心架构约束
1. **模块化与解耦**：每个服务角色应自包含。对 Docker 的依赖应通过标准包/服务检测处理，不与底层防火墙做强耦合绑定。
2. **零凭据**：模板和默认值中仅保留变量名占位符或本地回环测试值，禁止写入生产 IP 或真实凭证。
3. **单向引用**：本仓库作为能力提供方，绝不直接依赖父仓或其他同级子仓。
